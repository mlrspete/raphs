# Seed Structure

Seed files will live here once landing-page tests and admin bootstrap data are defined.

Landing-page tests for V0 are code-seeded from `lib/landing-tests/config.ts` with `npm run seed:landing-tests`.

Keep SQL seed files out of this folder until a later milestone needs database-only seed data.
